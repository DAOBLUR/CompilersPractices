func id ( ) { while id + ( null ) { return exec id ( ( int ) ) comma } } func id ( ) { for id + ( int ) comma id < int comma true { var id = int comma } } func id ( ) { if int < id { var id = int comma } } func id ( ) { var id = ( exec id ( int * id ) ) + id comma } main ( ) { var id = ( id != int ) comma jump jump }